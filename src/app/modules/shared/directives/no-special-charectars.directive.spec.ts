import { NoSpecialCharectarsDirective } from './no-special-charectars.directive';

describe('NoSpecialCharectarsDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSpecialCharectarsDirective();
    expect(directive).toBeTruthy();
  });
});
